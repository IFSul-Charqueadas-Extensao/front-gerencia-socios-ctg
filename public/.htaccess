Options -MultiViews
RewriteEngine On

# Redireciona tudo que nao for arquivo ou pasta real para o index.html
# Necessario para o React Router funcionar no Apache (SPA routing)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
